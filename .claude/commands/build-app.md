Rebuild the macOS .app bundle using PyInstaller.

Run this command:
```
python3 -m PyInstaller "Movie Finger Print.spec" -y
```

After the build completes, confirm it succeeded and report the path to the .app in `dist/`.
